# Baselines

Simple supervised classifier(s), one per category being compared, for the Phase 6
shared-approach-vs-baseline table. A small CNN is enough — it doesn't need to be
sophisticated (whitepaper Section 2.3 / workflow Phase 6, step 1). Fabric is the easiest
category to start with since public labeled data is most available.
